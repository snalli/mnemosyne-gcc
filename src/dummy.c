/* dummy.c — empty translation unit required by CMake to create a SHARED
 * library whose actual object code comes entirely from --whole-archive
 * static libraries (mcore, mtm, pmalloc).                               */
